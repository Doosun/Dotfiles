how to install everything that we need:
https://github.com/nvim-lua/kickstart.nvim?tab=readme-ov-file#linux-install
https://github.com/jesseduffield/lazygit?tab=readme-ov-file#manual
to install lazygit install homebrew:
https://brew.sh/
  brew install lazygit
  brew install nodejs
github easy install and setup on ubuntu:
  sudo apt install pass
  git config --global credential.credentialStore gpg
  gpg --gen-key # take user id as output as <gpg-id>
  pass init <gpg-id>
